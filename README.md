# mod_flexaccess

Optional Moodle activity module that lets a FlexAccess `temporary user` initiate self-activation inside a course while preserving the same Moodle user ID.

Depends on `auth_flexaccess` and `enrol_flexaccess`. Maturity: Beta (0.9.x); the self-activation form routes through the standard verification funnel.
